// database
datasource db {
    provider = "mongodb"
    url      = env("MONGODB_URI")
}

// generator
generator client {
    provider             = "prisma-client-py"
    recursive_type_depth = 5
}
